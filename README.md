# what's this?
A simple app for serving markdown files from the local filesystem

# usage

```
mvn package
java -jar target/mdserve-0.0.1-SNAPSHOT.one-jar.jar  port path-to-dir-with-markdown-files
```
